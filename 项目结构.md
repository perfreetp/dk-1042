# 项目文件结构

## 核心源代码

```
├── electron/                           # Electron 主进程
│   ├── main.ts                        # 主进程入口（文件扫描、IPC 处理）
│   ├── preload.ts                     # 预加载脚本（安全的 IPC 通信）
│   └── tsconfig.json                  # TypeScript 配置
│
├── src/                               # React 前端
│   ├── components/                    # React 组件
│   │   ├── LineageCanvas.tsx         # 血缘关系图画布（Cytoscape.js）
│   │   ├── FileCard.tsx              # 文件详情卡片
│   │   ├── ReviewModal.tsx           # 校对窗口
│   │   └── ChangePreviewModal.tsx    # 变更预览窗口
│   │
│   ├── utils/                         # 工具函数
│   │   └── lineageAnalyzer.ts        # 血缘分析核心算法
│   │
│   ├── types/                         # TypeScript 类型定义
│   │   └── index.ts                  # 所有类型定义
│   │
│   ├── App.tsx                       # 主应用组件
│   ├── main.tsx                      # React 入口
│   └── index.css                     # 全局样式
│
├── dist/                              # Vite 构建输出（前端）
├── dist-electron/                      # TypeScript 编译输出（Electron）
├── release/                           # 最终打包文件
│   ├── win-unpacked/                  # 便携版（无需安装）
│   │   └── 文件血缘图工具.exe         # 可执行文件
│   └── 文件血缘图工具 Setup 1.0.0.exe # 安装包
│
├── index.html                         # HTML 入口
├── package.json                       # NPM 依赖和脚本
├── tsconfig.json                      # TypeScript 配置
├── tsconfig.node.json                 # Vite TypeScript 配置
└── vite.config.ts                     # Vite 构建配置
```

## 核心模块说明

### 1. 血缘分析引擎 (src/utils/lineageAnalyzer.ts)
- `analyzeLineage()`: 主分析函数，识别所有血缘关系
- `analyzeVersionGroups()`: 识别同名版本文件
- `analyzeTableReferences()`: 分析表格引用关系
- `analyzeScriptDependencies()`: 分析脚本输入输出依赖
- 辅助函数：按扩展名过滤、分组等

### 2. 图形化画布 (src/components/LineageCanvas.tsx)
- 使用 Cytoscape.js 渲染血缘关系图
- 支持缩放、平移、拖拽
- 多种着色方案（按类型、负责人、日期）
- 点击节点显示详情

### 3. 文件卡片 (src/components/FileCard.tsx)
- 显示文件基本信息（路径、大小、修改时间）
- 版本号和负责人编辑
- 上游/下游依赖列表
- 置信度显示

### 4. 校对窗口 (src/components/ReviewModal.tsx)
- 关系列表过滤（全部/不确定/已确认/已废弃）
- 关系确认和标记
- 依赖原因补充

### 5. 变更预览 (src/components/ChangePreviewModal.tsx)
- 选择文件进行影响分析
- 递归查找下游影响
- 影响等级评估
- 导出清理清单

### 6. Electron 主进程 (electron/main.ts)
- 文件夹选择对话框
- 递归扫描文件系统
- 文件类型识别
- IPC 通信处理

## 技术栈

- **前端**: React 18.2 + TypeScript 5.2
- **图形**: Cytoscape.js 3.26
- **桌面**: Electron 28
- **构建**: Vite 5 + electron-builder 24
- **日志**: electron-log 4.4
