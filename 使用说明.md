# 文件血缘图工具使用指南

## 功能概览

这是一个功能完整的桌面应用文件血缘图工具，帮助数据整理人员追踪表格、脚本和报告之间的来源关系。

## 核心功能

### 1. 工作区管理
- **新建项目**: 创建新的血缘分析项目
- **导入文件夹**: 扫描指定文件夹下的所有文件
- **按扩展名分层**: 自动识别文件类型并分类

### 2. 血缘关系分析
自动识别以下关系：
- **表格引用**: Excel、CSV 等表格文件的引用关系
- **脚本输入输出**: Python、SQL 等脚本的输入输出依赖
- **同名版本**: 同一文件的不同版本（如 `file_v1.xlsx`, `file_v2.xlsx`）

### 3. 图形化画布
- **缩放功能**: 放大/缩小血缘关系图
- **拖拽交互**: 自由拖拽节点位置
- **折叠子链路**: 聚焦关键路径
- **着色方案**: 按负责人、日期或文件类型着色

### 4. 文件卡片
每个文件节点显示：
- 文件摘要信息
- 版本号（可编辑）
- 来源说明
- 上游依赖列表
- 下游影响分析

### 5. 校对窗口
- **人工确认**: 确认自动识别的关系
- **标记废弃**: 标记已废弃的依赖关系
- **补充原因**: 添加依赖关系的说明

### 6. 变更预览
- **影响分析**: 查看删除或替换文件将影响的报告
- **导出功能**: 
  - 文本格式清理清单
  - JSON 格式详细数据

## 文件类型识别

| 类型 | 扩展名 |
|------|--------|
| 表格 | .xlsx, .xls, .csv, .parquet, .db, .sqlite |
| 脚本 | .py, .sql, .r, .js, .sh, .bat |
| 报告 | .pdf, .docx, .html, .md, .pptx |
| 配置 | .json, .yaml, .yml, .toml, .ini, .conf |
| 数据 | .txt, .log, .xml |

## 安装和使用

### 安装应用
运行以下文件安装应用：
```
release\文件血缘图工具 Setup 1.0.0.exe
```

### 直接运行（无需安装）
```
release\win-unpacked\文件血缘图工具.exe
```

## 技术架构

- **前端框架**: React 18 + TypeScript
- **图形引擎**: Cytoscape.js
- **桌面框架**: Electron 28
- **构建工具**: Vite 5
- **打包工具**: electron-builder

## 项目结构

```
├── electron/          # Electron 主进程代码
│   ├── main.ts       # 主进程入口
│   └── preload.ts    # 预加载脚本
├── src/              # React 前端代码
│   ├── components/   # React 组件
│   ├── utils/        # 工具函数
│   └── types/        # TypeScript 类型定义
├── dist/             # 构建输出（前端）
├── dist-electron/    # 构建输出（Electron）
└── release/          # 最终打包文件
```

## 使用示例

### 示例 1: 分析数据管道
1. 新建项目
2. 导入包含以下文件的文件夹：
   - `raw_data.csv` (源数据)
   - `process_data.py` (处理脚本)
   - `cleaned_data.csv` (输出数据)
   - `report.xlsx` (最终报告)
3. 系统会自动识别血缘关系
4. 在画布上查看关系图

### 示例 2: 变更影响评估
1. 在变更预览页面选择要删除的文件
2. 查看将受影响的报告列表
3. 导出清理清单
4. 通知相关人员

## 注意事项

- 应用需要访问文件系统来扫描文件夹
- 大型项目的血缘分析可能需要较长时间
- 建议定期保存项目并导出备份

## 版本信息

- 版本号: 1.0.0
- 构建日期: 2026-06-12
